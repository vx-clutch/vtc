#import "std.io"

proc add(int x, int y) int ::
  return x + y;

proc main(void) int ::
  printfn("hello %d", add(34, 35));
  return 0;

(*
#include <stdio.h>

int add (int x, int y)
{
  return x + y;
}

int main(void)
{
  printf("hello %d", add(34, 35));
  return 0;
}
*)
